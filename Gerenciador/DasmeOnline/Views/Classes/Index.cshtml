@{
    Layout = "~/Views/_Layouts/_Dasme.cshtml";
}

<header class="major">
    <h2>Bem Vindo @ViewBag.LOGIN</h2>
</header>
<div class="row">
    <div class="col-md-8">
        <h4>Classes criadas:</h4>
    </div>
    <div class="col-md-4" align="right">
        <a href="~/Classes/Create">
            <input type="button" id="txtCreateClasse" value="Nova Classe" />
        </a>
    </div>
</div>
<div class="row">
    <div class="col-12">
        <table border="3">
            <tr>
                <td>Nome</td>
                <td>Descrição</td>
                <td>Ativo</td>
                <td>Editar</td>
                <td>Excluir</td>
            </tr>
            @foreach (var item in ViewBag.TabClasses)
            {
                <tr>
                    <td>@item.CLASSE</td>
                    <td>@item.DESCRICAO</td>
                    <td>
                        @if (item.ATIVO == 1)
                        {
                            <input type="checkbox" class="form-check-input" name="ATIVO" value="" id="flexSwitchCheckDefault">
                        }
                        else
                        {
                            <input type="checkbox" class="form-check-input" name="ATIVO" value="" id="flexSwitchCheckDefault">
                        }
                    </td>
                    <td><a class="btn btn-info" href="~/Classes/Update/@item.COD"><span class="fa fa-edit"></span></a></td>
                    <td><a class="btn btn-danger" href="~/Classes/Delete/@item.COD"><span class="fa fa-book"></span></a></td>
                </tr>
            }
        </table>
    </div>
</div>

